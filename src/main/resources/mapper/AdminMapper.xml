<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kinben233.DDMall.mapper.AdminMapper">
    <select id="get" resultType="Admin" parameterType="map">
        select * from Admin_view where name=#{name} and password=#{password}
  </select>
</mapper>